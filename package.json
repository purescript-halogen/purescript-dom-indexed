{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "spago build --strict --pedantic-packages --ensure-ranges"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
